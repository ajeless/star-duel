{
  "name": "star-duel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "start:dev": "node scripts/dev-server.mjs start",
    "stop:dev": "node scripts/dev-server.mjs stop",
    "status:dev": "node scripts/dev-server.mjs status",
    "smoke": "node scripts/browser-smoke.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright-core": "^1.55.0",
    "vite": "^8.0.0"
  }
}
